# SmartQuery AI - 实现计划

| 项目 | 内容 |
|-----|------|
| **版本** | 1.0.0 |
| **状态** | 初稿 |
| **创建日期** | 2026-05-30 |
| **基于文档** | specs/week2/0001-需求分析与设计.md |

---

## 目录

1. [项目架构概览](#1-项目架构概览)
2. [项目结构](#2-项目结构)
3. [开发阶段规划](#3-开发阶段规划)
4. [后端实现计划](#4-后端实现计划)
5. [前端实现计划](#5-前端实现计划)
6. [数据库设计](#6-数据库设计)
7. [测试策略](#7-测试策略)
8. [部署计划](#8-部署计划)

---

## 1. 项目架构概览

```
week2/
├── smartquery-backend/          # Rust 后端
│   ├── src/
│   │   ├── api/                 # API 层
│   │   ├── services/            # 业务逻辑层
│   │   ├── models/              # 数据模型
│   │   ├── repositories/         # 数据访问层
│   │   ├── middleware/           # 中间件
│   │   └── utils/               # 工具函数
│   └── migrations/              # 数据库迁移
│
├── smartquery-frontend/         # React 前端
│   ├── src/
│   │   ├── components/           # 组件
│   │   ├── pages/               # 页面
│   │   ├── stores/              # 状态管理
│   │   ├── services/            # API 调用
│   │   └── hooks/               # 自定义 Hooks
│   └── public/
│
└── docker/                      # Docker 配置
```

---

## 2. 项目结构

### 2.1 后端项目结构 (Rust)

```
smartquery-backend/
├── Cargo.toml
├── config.yaml                   # 配置文件
├── .env.example                  # 环境变量示例
│
├── src/
│   ├── main.rs                  # 程序入口
│   ├── lib.rs                   # 库入口
│   │
│   ├── config.rs               # 配置加载模块
│   ├── error.rs               # 错误类型定义
│   ├── state.rs               # 应用状态
│   │
│   ├── api/                    # === API 层 ===
│   │   ├── mod.rs
│   │   ├── auth.rs            # 认证接口 (login/register/token)
│   │   ├── users.rs           # 用户接口
│   │   ├── connections.rs     # 数据库连接接口
│   │   ├── sql_execute.rs     # SQL执行接口
│   │   ├── nl_convert.rs      # NL转SQL接口
│   │   ├── conversations.rs   # 对话管理接口
│   │   ├── charts.rs          # 图表接口
│   │   └── metrics.rs         # 指标接口
│   │
│   ├── services/               # === 业务逻辑层 ===
│   │   ├── mod.rs
│   │   ├── auth_service.rs    # 认证服务
│   │   ├── llm_client.rs     # LLM客户端
│   │   ├── sql_executor.rs    # SQL执行器
│   │   ├── sql_analyzer.rs    # SQL AST分析
│   │   ├── schema_retrieval.rs# Schema检索(RAG)
│   │   ├── chart_generator.rs # 图表生成
│   │   └── data_masker.rs     # 数据脱敏
│   │
│   ├── models/                 # === 数据模型 ===
│   │   ├── mod.rs
│   │   ├── user.rs
│   │   ├── connection.rs
│   │   ├── query.rs
│   │   ├── conversation.rs
│   │   ├── semantic.rs
│   │   └── metric.rs
│   │
│   ├── repositories/           # === 数据访问层 ===
│   │   ├── mod.rs
│   │   ├── user_repo.rs
│   │   ├── connection_repo.rs
│   │   ├── query_repo.rs
│   │   └── conversation_repo.rs
│   │
│   ├── middleware/            # === 中间件 ===
│   │   ├── mod.rs
│   │   ├── auth.rs            # JWT认证
│   │   ├── logging.rs         # 请求日志
│   │   └── error_handler.rs   # 错误处理
│   │
│   └── utils/                 # === 工具函数 ===
│       ├── mod.rs
│       ├── password.rs         # 密码哈希
│       ├── jwt.rs              # JWT工具
│       └── validation.rs       # 输入验证
│
└── migrations/                # SQLx 迁移文件
    └── 001_initial_schema.sql
```

### 2.2 前端项目结构 (React)

```
smartquery-frontend/
├── package.json
├── vite.config.ts
├── tsconfig.json
│
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── index.css
│   │
│   ├── api/                   # API 定义
│   │   ├── client.ts          # Axios 实例
│   │   ├── auth.ts
│   │   ├── connections.ts
│   │   ├── sql.ts
│   │   ├── chat.ts
│   │   └── charts.ts
│   │
│   ├── components/             # 通用组件
│   │   ├── Layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── MainLayout.tsx
│   │   ├── Editor/
│   │   │   └── SqlEditor.tsx  # Monaco 编辑器封装
│   │   ├── Charts/
│   │   │   ├── ChartRenderer.tsx
│   │   │   └── ChartConfig.tsx
│   │   └── Common/
│   │       ├── Button.tsx
│   │       ├── Modal.tsx
│   │       └── Table.tsx
│   │
│   ├── pages/                 # 页面组件
│   │   ├── Login.tsx
│   │   ├── Dashboard.tsx
│   │   ├── SqlMode/
│   │   │   ├── SqlWorkspace.tsx
│   │   │   ├── ConnectionPanel.tsx
│   │   │   ├── QueryResult.tsx
│   │   │   └── ExecutionPlan.tsx
│   │   ├── ChatMode/
│   │   │   ├── ChatWorkspace.tsx
│   │   │   ├── ChatPanel.tsx
│   │   │   ├── SqlPreview.tsx
│   │   │   └── ChatHistory.tsx
│   │   └── Admin/
│   │       ├── UserManagement.tsx
│   │       ├── RoleManagement.tsx
│   │       └── AuditLog.tsx
│   │
│   ├── stores/                # Zustand 状态管理
│   │   ├── authStore.ts
│   │   ├── connectionStore.ts
│   │   ├── queryStore.ts
│   │   └── chatStore.ts
│   │
│   ├── hooks/                 # 自定义 Hooks
│   │   ├── useAuth.ts
│   │   ├── useSqlExecute.ts
│   │   ├── useNlConvert.ts
│   │   └── useChart.ts
│   │
│   ├── types/                 # TypeScript 类型定义
│   │   ├── api.ts
│   │   ├── models.ts
│   │   └── charts.ts
│   │
│   └── utils/                 # 工具函数
│       ├── formatters.ts
│       └── validators.ts
│
└── public/
    └── favicon.ico
```

---

## 3. 开发阶段规划

### 3.1 阶段概览

| 阶段 | 周次 | 目标 | 后端模块 | 前端模块 |
|-----|-----|------|---------|---------|
| **Phase 1: 基础搭建** | Week 1 | 项目初始化、认证基础 | 项目骨架、JWT认证 | React 项目搭建 |
| **Phase 2: SQL 模式** | Week 1-2 | SQL 编辑器与执行 | SQL执行服务、AST分析 | SQL工作区 |
| **Phase 3: NL 模式** | Week 2-3 | 自然语言转SQL | LLM客户端、Schema检索 | 对话工作区 |
| **Phase 4: 图表功能** | Week 3-4 | 数据可视化 | 图表生成服务 | 图表组件 |
| **Phase 5: 语义层** | Week 4-5 | 元数据管理 | 语义服务、指标服务 | 语义配置页面 |
| **Phase 6: 权限系统** | Week 5-6 | RBAC 实现 | 用户服务、权限校验 | 管理后台 |
| **Phase 7: 生产优化** | Week 6 | 性能优化、部署 | 缓存、限流、监控 | 响应式优化 |

### 3.2 详细里程碑

```
Week 1
├── M1.1: Rust 项目初始化
│   ├── Cargo 项目创建
│   ├── 依赖配置 (Axum, SQLx, sqlparser, JWT)
│   ├── 项目结构搭建
│   └── 配置文件加载
│
├── M1.2: React 项目初始化
│   ├── Vite + React 19 项目创建
│   ├── Tailwind CSS 配置
│   ├── Monaco Editor 集成
│   └── 路由配置
│
├── M1.3: 基础认证模块
│   ├── JWT Token 生成与验证
│   ├── 登录/注册 API
│   ├── 登录页面
│   └── 路由守卫

Week 2
├── M2.1: 数据库连接管理
│   ├── 连接 CRUD API
│   ├── 连接测试功能
│   ├── 连接面板组件
│   └── 状态管理
│
├── M2.2: SQL 执行服务
│   ├── SQL 执行 API
│   ├── AST 安全分析
│   ├── 多数据库支持 (MySQL/PostgreSQL)
│   └── 查询历史记录
│
├── M2.3: SQL 编辑器
│   ├── Monaco 集成
│   ├── 语法高亮
│   ├── 自动补全
│   └── 执行结果展示

Week 3
├── M3.1: LLM 客户端
│   ├── OpenAI API 集成
│   ├── 模型路由
│   └── Token 计数
│
├── M3.2: NL 转 SQL 服务
│   ├── Prompt 工程
│   ├── Schema 检索
│   ├── SQL 生成
│   └── 方言适配
│
├── M3.3: 对话界面
│   ├── ChatGPT 风格 UI
│   ├── 消息列表
│   ├── SQL 预览卡片
│   └── 确认执行流程

Week 4
├── M4.1: 图表生成服务
│   ├── 数据分析
│   ├── 图表类型推荐
│   ├── ECharts 配置生成
│   └── 数据洞察
│
├── M4.2: 图表组件
│   ├── 图表渲染器
│   ├── 类型切换
│   ├── 交互功能
│   └── 导出功能
│
├── M4.3: Schema 检索增强
│   ├── pgvector 集成
│   ├── 向量嵌入
│   ├── 相似度检索
│   └── RAG 实现

Week 5
├── M5.1: 语义层服务
│   ├── 表结构管理
│   ├── 语义词典
│   ├── 同义词配置
│   └── NL 增强
│
├── M5.2: 指标管理
│   ├── 指标 CRUD
│   ├── 计算公式解析
│   ├── 血缘关系
│   └── 指标服务 API
│
├── M5.3: 语义配置页面
│   ├── 表结构浏览器
│   ├── 字段配置
│   ├── 术语管理
│   └── 指标配置

Week 6
├── M6.1: RBAC 系统
│   ├── 角色管理
│   ├── 权限定义
│   ├── 数据权限
│   └── 权限中间件
│
├── M6.2: 管理后台
│   ├── 用户管理
│   ├── 角色管理
│   ├── 审计日志
│   └── 连接管理
│
├── M6.3: 性能优化
│   ├── Redis 缓存
│   ├── 连接池优化
│   ├── 限流熔断
│   └── 前端优化

Week 7
├── M7.1: 生产部署
│   ├── Docker 配置
│   ├── K8s 配置
│   ├── 环境配置
│   └── CI/CD 流程
│
├── M7.2: 监控告警
│   ├── Prometheus 指标
│   ├── Grafana 面板
│   ├── 日志聚合
│   └── 告警规则
│
└── M7.3: 文档与测试
    ├── API 文档 (Swagger)
    ├── 用户手册
    ├── 集成测试
    └── E2E 测试
```

---

## 4. 后端实现计划

### 4.1 模块依赖关系

```
┌─────────────────────────────────────────────────────────────────┐
│                         API 层 (api/)                          │
│  auth.rs → users.rs → connections.rs → sql_execute.rs          │
│           → nl_convert.rs → conversations.rs → charts.rs       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       服务层 (services/)                        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐│
│  │ auth_service │  │ llm_client   │  │ sql_analyzer         ││
│  │ (JWT/密码)   │  │ (OpenAI)     │  │ (AST安全检测)        ││
│  └──────────────┘  └──────────────┘  └──────────────────────┘│
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐│
│  │ sql_executor │  │ schema_retriev│ │ chart_generator      ││
│  │ (执行查询)   │  │ (RAG检索)    │  │ (图表推荐)           ││
│  └──────────────┘  └──────────────┘  └──────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       数据层 (repositories/)                    │
│  user_repo → connection_repo → query_repo → conversation_repo  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         数据库 (PostgreSQL)                     │
│  users | connections | query_history | conversations | messages│
│  semantic_definitions | metrics | audit_logs | schema_embeddings│
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 核心模块实现顺序

#### Step 1: 基础设施 (Day 1-2)

**config.rs** - 配置管理
```
职责：
- 加载 config.yaml
- 环境变量覆盖
- 配置验证

实现要点：
- 使用 serde_yaml 解析 YAML
- 支持 .env 文件
- 配置项：数据库、Redis、LLM、日志
```

**error.rs** - 错误类型
```
职责：
- 统一错误类型
- 错误转 JSON
- 错误日志

实现要点：
- 定义 AppError 枚举
- 实现 From trait
- 区分业务错误和系统错误
```

**state.rs** - 应用状态
```
职责：
- 管理共享状态
- 数据库连接池
- LLM 客户端

实现要点：
- Arc<AppState>
- SQLx PgPool
- Reqwest Client
```

#### Step 2: 数据模型 (Day 2-3)

**models/** - 数据结构
```
实现顺序：
1. user.rs      - 用户与角色
2. connection.rs - 数据库连接
3. query.rs      - 查询历史
4. conversation.rs - 对话与消息
5. semantic.rs   - 语义定义
6. metric.rs     - 指标定义
```

#### Step 3: 数据访问层 (Day 3-4)

**repositories/** - 数据库操作
```
实现顺序：
1. user_repo.rs       - 用户 CRUD
2. connection_repo.rs - 连接 CRUD
3. query_repo.rs      - 查询历史
4. conversation_repo.rs - 对话管理
```

#### Step 4: 工具模块 (Day 4)

**utils/** - 工具函数
```
1. password.rs    - Argon2 密码哈希
2. jwt.rs         - JWT 编码/解码
3. validation.rs  - 输入验证
```

#### Step 5: 中间件 (Day 4-5)

**middleware/**
```
1. auth.rs         - JWT 认证中间件
2. logging.rs      - 请求日志中间件
3. error_handler.rs - 统一错误处理
```

#### Step 6: 服务层 (Day 5-8)

**services/**
```
1. auth_service.rs    - 认证逻辑 (登录/注册/Token刷新)
2. sql_executor.rs    - SQL 执行 (多数据库支持)
3. sql_analyzer.rs    - AST 分析 (安全检测)
4. llm_client.rs      - LLM 调用 (OpenAI)
5. schema_retrieval.rs - Schema 检索 (RAG)
6. chart_generator.rs - 图表生成 (推荐算法)
7. data_masker.rs     - 数据脱敏
```

#### Step 7: API 层 (Day 8-12)

**api/** - HTTP 端点
```
实现顺序：
1. auth.rs         - POST /api/v1/auth/login
                   - POST /api/v1/auth/register
                   - POST /api/v1/auth/refresh
                   - POST /api/v1/auth/logout

2. users.rs        - GET    /api/v1/users
                   - GET    /api/v1/users/:id
                   - PUT    /api/v1/users/:id
                   - DELETE /api/v1/users/:id
                   - PUT    /api/v1/users/:id/password

3. connections.rs  - GET    /api/v1/connections
                   - POST   /api/v1/connections
                   - GET    /api/v1/connections/:id
                   - PUT    /api/v1/connections/:id
                   - DELETE /api/v1/connections/:id
                   - POST   /api/v1/connections/:id/test
                   - PUT    /api/v1/connections/:id/default

4. sql_execute.rs   - POST   /api/v1/sql/execute
                   - POST   /api/v1/sql/format
                   - GET    /api/v1/sql/history
                   - GET    /api/v1/sql/explain

5. nl_convert.rs    - POST   /api/v1/nl/convert
                   - POST   /api/v1/nl/execute

6. conversations.rs - GET    /api/v1/conversations
                   - POST   /api/v1/conversations
                   - GET    /api/v1/conversations/:id
                   - DELETE /api/v1/conversations/:id
                   - GET    /api/v1/conversations/:id/messages
                   - POST   /api/v1/conversations/:id/messages

7. charts.rs        - GET    /api/v1/charts/recommend
                   - POST   /api/v1/charts/generate

8. metrics.rs       - GET    /api/v1/metrics
                   - POST   /api/v1/metrics
                   - GET    /api/v1/metrics/:id
                   - PUT    /api/v1/metrics/:id
                   - DELETE /api/v1/metrics/:id
```

### 4.3 关键实现细节

#### 4.3.1 SQL AST 安全分析

```rust
// services/sql_analyzer.rs

// 检测规则优先级：
// 1. 语法解析失败 → 拒绝
// 2. DDL 操作 (CREATE/DROP/ALTER/TRUNCATE) → 拒绝
// 3. DML 操作 (INSERT/UPDATE/DELETE) → 拒绝
// 4. 危险函数 (LOAD_FILE, INTO OUTFILE, SLEEP...) → 警告
// 5. 跨库查询 → 警告或拒绝
// 6. 注释注入 → 拒绝
```

#### 4.3.2 LLM 提示词模板

```rust
// services/llm_client.rs

const NL_TO_SQL_PROMPT: &str = r#"
你是一个专业的 SQL 专家。请根据以下 Schema 信息，将用户问题转换为 SQL 查询。

## Schema 信息
{schema}

## 数据库类型
{dialect}

## 用户问题
{question}

## 要求
1. 只生成 SELECT 查询，不要生成 INSERT/UPDATE/DELETE
2. 考虑性能，必要时添加 LIMIT
3. 添加中文注释解释查询逻辑
4. 如果问题不明确，进行合理假设

请按以下 JSON 格式返回：
{
  "sql": "生成的 SQL",
  "explanation": "查询逻辑的中文解释",
  "confidence": 0.0-1.0 的置信度
}
"#;
```

#### 4.3.3 Schema RAG 检索

```rust
// services/schema_retrieval.rs

// 检索流程：
// 1. 用户 NL 问题 → 向量化
// 2. pgvector 相似度检索 (top_k=5)
// 3. 组装上下文 Prompt
// 4. LLM 生成 SQL
```

#### 4.3.4 图表推荐算法

```rust
// services/chart_generator.rs

// 推荐规则：
// - 时间序列数据 (>5个时间点) → 折线图
// - 分类对比 (<10类) → 柱状图
// - 占比分析 → 饼图/环形图
// - 转化漏斗 → 漏斗图
// - 数值分布 → 直方图/箱线图
// - 地理数据 → 地图热力图
```

---

## 5. 前端实现计划

### 5.1 组件依赖关系

```
App.tsx
├── MainLayout
│   ├── Header
│   │   └── UserMenu
│   │
│   ├── Sidebar
│   │   ├── ConnectionTree
│   │   ├── SchemaBrowser
│   │   └── SemanticPanel
│   │
│   └── Router
│       ├── /login → LoginPage
│       │
│       ├── /sql → SqlWorkspace
│       │   ├── SqlEditor (Monaco)
│       │   ├── ConnectionSelector
│       │   ├── QueryResultTable
│       │   ├── ExecutionPlan
│       │   └── ChartPanel
│       │
│       ├── /chat → ChatWorkspace
│       │   ├── ChatHistory
│       │   ├── ChatMessages
│       │   ├── SqlPreviewModal
│       │   └── ChatResultPanel
│       │
│       └── /admin → AdminLayout
│           ├── UserManagement
│           ├── RoleManagement
│           └── AuditLog
```

### 5.2 页面实现顺序

#### Phase 1: 基础架构 (Week 1)

**Day 1-2: 项目初始化**
```
1. Vite + React 19 项目创建
2. TypeScript 配置
3. Tailwind CSS 集成
4. 目录结构创建
5. 路由配置 (React Router)
```

**Day 3-4: 状态管理**
```
1. Zustand store 基础结构
2. authStore - 认证状态
3. API client 配置 (Axios)
4. 请求拦截器 (Token)
```

**Day 5: 布局组件**
```
1. MainLayout 组件
2. Header 组件
3. Sidebar 组件
4. 路由守卫
```

#### Phase 2: 登录与认证 (Week 1-2)

**Day 6-7: 登录页面**
```
1. LoginPage 组件
2. 表单验证
3. 登录 API 集成
4. 错误处理
5. Token 存储 (localStorage)
```

#### Phase 3: SQL 模式 (Week 2)

**Day 8-10: SQL 编辑器**
```
1. Monaco Editor 封装
2. SQL 语法高亮 (MySQL/PostgreSQL)
3. 自动补全 (表名、列名)
4. 快捷键绑定
5. 主题适配
```

**Day 11-12: 执行结果展示**
```
1. 表格组件
2. 分页功能
3. 列排序
4. 数据导出 (CSV/JSON)
5. 空状态处理
```

**Day 13-14: 连接管理**
```
1. ConnectionTree 组件
2. 连接创建/编辑表单
3. 连接测试
4. 连接状态指示
5. Schema 浏览器
```

#### Phase 4: 对话模式 (Week 3)

**Day 15-17: 对话界面**
```
1. ChatMessages 组件
2. 消息气泡样式
3. Markdown 渲染
4. 代码块高亮
5. 加载状态动画
```

**Day 18-19: SQL 预览**
```
1. SqlPreviewModal 组件
2. SQL 语法高亮
3. 执行确认流程
4. 解释文本展示
```

**Day 20-21: 对话历史**
```
1. ChatHistory 侧边栏
2. 会话列表
3. 新建/删除会话
4. 会话切换
```

#### Phase 5: 图表功能 (Week 4)

**Day 22-24: 图表组件**
```
1. ChartRenderer 封装 ECharts
2. 图表类型切换
3. 数据映射配置
4. 交互功能 (缩放、筛选)
```

**Day 25-26: 图表推荐**
```
1. 推荐图表 UI
2. 类型切换器
3. 图表下载 (PNG/SVG)
4. 全屏展示
```

#### Phase 6: 语义层 (Week 5)

**Day 27-29: 语义配置**
```
1. 表结构面板
2. 字段备注编辑
3. 术语管理
4. 同义词配置
```

**Day 30-31: 指标管理**
```
1. 指标列表
2. 指标创建/编辑
3. 维度配置
4. 预览功能
```

#### Phase 7: 管理后台 (Week 6)

**Day 32-34: 用户管理**
```
1. 用户列表
2. 用户表单
3. 角色分配
4. 状态切换
```

**Day 35-36: 审计日志**
```
1. 日志列表
2. 筛选功能
3. 导出功能
```

### 5.3 关键组件实现

#### 5.3.1 Monaco SQL 编辑器

```tsx
// components/Editor/SqlEditor.tsx

interface SqlEditorProps {
  value: string;
  onChange: (value: string) => void;
  dialect: 'mysql' | 'postgresql' | 'clickhouse';
  onExecute?: () => void;
  tables?: TableInfo[];  // 用于自动补全
}

// 功能点：
// - 语法高亮 (自定义语法)
// - 表名/列名补全 (CompletionProvider)
// - 格式化 (Prettier)
// - Ctrl+Enter 执行快捷键
// - 错误标注 (Diagnostic)
// - 多标签页支持
```

#### 5.3.2 对话消息组件

```tsx
// components/Chat/ChatMessage.tsx

interface ChatMessageProps {
  role: 'user' | 'assistant';
  content: string;
  sql?: string;
  sqlExplanation?: string;
  chartConfig?: ChartConfig;
  executionResult?: QueryResult;
  onConfirmExecute?: () => void;
}

// 渲染逻辑：
// - 用户消息: 右侧气泡
// - AI 消息: 左侧气泡 + Markdown
// - SQL 预览: 代码块 + 执行按钮
// - 图表结果: ChartRenderer
// - 一句话洞察: 文字卡片
```

#### 5.3.3 状态管理

```tsx
// stores/chatStore.ts

interface ChatStore {
  // 状态
  conversations: Conversation[];
  currentConversationId: string | null;
  messages: Message[];
  isLoading: boolean;
  
  // 操作
  createConversation: () => Promise<void>;
  sendMessage: (content: string) => Promise<void>;
  executeGeneratedSql: (sql: string) => Promise<void>;
  switchConversation: (id: string) => void;
}
```

---

## 6. 数据库设计

### 6.1 表结构概览

```
┌──────────────────┐     ┌──────────────────┐
│      users       │     │  audit_logs      │
├──────────────────┤     ├──────────────────┤
│ id (PK)          │     │ id (PK)          │
│ username         │     │ user_id (FK)     │
│ email            │     │ action           │
│ password_hash    │     │ resource_type    │
│ role             │     │ resource_id      │
│ is_active        │     │ details (JSONB)  │
│ created_at       │     │ ip_address       │
│ updated_at       │     │ created_at       │
└────────┬─────────┘     └──────────────────┘
         │
         │ 1:N
         ▼
┌──────────────────┐     ┌──────────────────┐
│database_connections│   │ query_history    │
├──────────────────┤     ├──────────────────┤
│ id (PK)          │     │ id (PK)          │
│ name             │     │ connection_id(FK)│
│ db_type          │     │ user_id (FK)     │
│ host             │     │ sql_text         │
│ port             │     │ status           │
│ database_name    │     │ duration_ms      │
│ username         │     │ row_count        │
│ encrypted_password│    │ error_message    │
│ is_default       │     │ created_at       │
│ created_by (FK)  │     └──────────────────┘
│ created_at       │
└────────┬─────────┘
         │
         │ 1:N
         ▼
┌──────────────────┐     ┌──────────────────┐
│ semantic_definitions│ │  conversations   │
├──────────────────┤     ├──────────────────┤
│ id (PK)          │     │ id (PK)          │
│ connection_id(FK)│     │ user_id (FK)     │
│ table_name       │     │ title            │
│ column_name      │     │ created_at       │
│ business_name    │     │ updated_at       │
│ business_desc    │◄────┤                 │
│ synonyms (JSONB) │     └────────┬─────────┘
│ is_active        │              │ 1:N
│ created_at       │              ▼
│ updated_at       │     ┌──────────────────┐
└──────────────────┘     │    messages      │
                         ├──────────────────┤
┌──────────────────┐     │ id (PK)          │
│   metrics        │     │ conversation_id  │
├──────────────────┤     │ (FK)             │
│ id (PK)          │     │ role             │
│ name             │     │ content          │
│ code             │     │ generated_sql    │
│ description      │     │ sql_explanation  │
│ expression       │     │ execution_result │
│ dimensions (JSONB│     │ (JSONB)          │
│ unit             │     │ chart_config     │
│ format_type      │     │ (JSONB)          │
│ created_by (FK)  │     │ created_at       │
│ created_at       │     └──────────────────┘
│ updated_at       │
└──────────────────┘     ┌──────────────────┐
                         │ schema_embeddings│
┌──────────────────┐     ├──────────────────┤
│   connections    │     │ id (PK)          │
│   (共享连接)     │     │ connection_id    │
└──────────────────┘     │ (FK)             │
                         │ table_name       │
                         │ column_names     │
                         │ embedding        │
                         │ (VECTOR)         │
                         │ created_at       │
                         └──────────────────┘
```

### 6.2 迁移脚本顺序

```sql
-- 001_initial_schema.sql

-- 1. 基础表
CREATE TABLE users (...);
CREATE TABLE audit_logs (...);

-- 2. 连接相关
CREATE TABLE database_connections (...);
CREATE TABLE semantic_definitions (...);
CREATE TABLE schema_embeddings (...);

-- 3. 查询历史
CREATE TABLE query_history (...);

-- 4. 对话相关
CREATE TABLE conversations (...);
CREATE TABLE messages (...);

-- 5. 指标
CREATE TABLE metrics (...);

-- 6. 索引
CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_query_history_user_id ON query_history(user_id);
CREATE INDEX idx_conversations_user_id ON conversations(user_id);
CREATE INDEX idx_messages_conversation_id ON messages(conversation_id);
CREATE INDEX idx_schema_embeddings_embedding ON schema_embeddings USING ivfflat(...);
```

---

## 7. 测试策略

### 7.1 测试分层

```
┌─────────────────────────────────────────────────┐
│              E2E 测试 (Playwright)               │
│   完整用户流程：登录 → 执行查询 → 查看结果        │
├─────────────────────────────────────────────────┤
│              集成测试 (Rust: actix-rt)           │
│   API 端到端：HTTP 请求 → 业务逻辑 → 数据库       │
├─────────────────────────────────────────────────┤
│              单元测试 (Rust: #[test])           │
│   模块隔离：services, repositories, utils        │
├─────────────────────────────────────────────────┤
│              组件测试 (React Testing Library)    │
│   组件行为：props → state → render → events      │
└─────────────────────────────────────────────────┘
```

### 7.2 测试覆盖重点

**后端测试**
```
1. SQL AST 分析器
   - 允许 SELECT 语句
   - 拒绝 DML/DDL 语句
   - 拒绝危险函数
   - 注入检测

2. JWT 认证
   - Token 生成验证
   - Token 过期处理
   - 权限校验

3. LLM 客户端 (Mock)
   - Prompt 构建
   - 响应解析
   - 错误处理

4. API 端点
   - 请求验证
   - 权限检查
   - 响应格式
```

**前端测试**
```
1. SQL 编辑器
   - 值变化回调
   - 快捷键绑定
   - 补全触发

2. 对话组件
   - 消息渲染
   - 加载状态
   - 错误提示

3. 图表组件
   - 数据映射
   - 类型切换
   - 导出功能
```

### 7.3 测试数据

```sql
-- 测试用数据库连接
INSERT INTO database_connections (name, db_type, host, port, ...)
VALUES ('测试MySQL', 'mysql', 'localhost', 3306, ...);

-- 测试用户
INSERT INTO users (username, email, password_hash, role)
VALUES ('test_user', 'test@example.com', '$argon2...', 'analyst');

-- 测试对话
INSERT INTO conversations (user_id, title)
VALUES ('uuid', '测试对话');
```

---

## 8. 部署计划

### 8.1 Docker 配置

```yaml
# docker/docker-compose.yml

services:
  # 后端
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/smartquery
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  # 前端
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  # PostgreSQL (元数据)
  db:
    image: postgres:16
    environment:
      - POSTGRES_DB=smartquery
      - POSTGRES_PASSWORD=password
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Redis (缓存)
  redis:
    image: redis:7-alpine
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
```

### 8.2 环境配置

**开发环境 (.env.development)**
```
DATABASE_URL=postgresql://postgres:password@localhost:5432/smartquery_dev
REDIS_URL=redis://localhost:6379
LLM_API_KEY=sk-test-xxx
LLM_BASE_URL=https://api.openai.com/v1
LOG_LEVEL=debug
RUST_LOG=debug
```

**生产环境 (.env.production)**
```
DATABASE_URL=postgresql://user:pass@prod-db:5432/smartquery
REDIS_URL=redis://prod-redis:6379
LLM_API_KEY=sk-prod-xxx
LOG_LEVEL=info
RUST_LOG=info
```

### 8.3 Kubernetes 部署

```yaml
# k8s/backend-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartquery-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: smartquery-backend
  template:
    metadata:
      labels:
        app: smartquery-backend
    spec:
      containers:
        - name: backend
          image: smartquery/backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: smartquery-secrets
                  key: database-url
```

### 8.4 监控指标

```rust
// 暴露给 Prometheus 的指标

// HTTP 请求
http_requests_total{method, endpoint, status}
http_request_duration_seconds{method, endpoint}

// SQL 执行
sql_execute_total{connection_type, status}
sql_execute_duration_seconds{connection_type}

// LLM 调用
llm_requests_total{model, status}
llm_tokens_total{model, type}
llm_request_duration_seconds{model}

// 数据库连接
db_pool_connections_active{pool}
db_pool_connections_idle{pool}
```

---

## 附录

### A. 开发依赖检查清单

**Rust 工具链**
- [ ] Rust 1.85+
- [ ] Cargo
- [ ] cargo-watch (热重载)
- [ ] sqlx-cli (数据库迁移)
- [ ] rustfmt (代码格式化)
- [ ] clippy (代码检查)

**前端工具链**
- [ ] Node.js 20+
- [ ] pnpm (包管理器)
- [ ] Vite
- [ ] TypeScript 5+
- [ ] ESLint
- [ ] Prettier

**开发服务**
- [ ] PostgreSQL 16+
- [ ] Redis 7+
- [ ] Docker Desktop
- [ ] (可选) pgvector 扩展

### B. 参考资料

- [Axum 文档](https://docs.rs/axum/latest/axum/)
- [SQLx 文档](https://docs.rs/sqlx/latest/sqlx/)
- [sqlparser-rs](https://github.com/sqlparser-rs/sqlparser-rs)
- [React 19](https://react.dev/blog/2024/04/25/react-19)
- [Monaco Editor](https://microsoft.github.io/monaco-editor/)
- [ECharts](https://echarts.apache.org/)
- [Zustand](https://zustand-demo.pmnd.rs/)

---

> 文档版本：1.0.0
> 创建时间：2026-05-30
> 基于文档：specs/week2/0001-需求分析与设计.md
